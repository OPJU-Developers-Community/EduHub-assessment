import Button from "@/atoms/Button";
import { AddIcon } from "@/atoms/icons";

export default function Components() {
  return (
    <>
      <Button
      // isLoading
      // isDisable
      // onClick={() => console.log("slk")}
      // startIcon={<AddIcon className="text-purple-500" />}
      >
        Click me
      </Button>
    </>
  );
}
